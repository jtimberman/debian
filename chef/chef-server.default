# Default user and group to run chef-server
USER=www-data
GROUP=www-data

# Number of merb nodes to run, must be at least 2 for openid
CLUSTER=2

# Configuration file.
CONFIG=/etc/chef/server.rb

# Adapter to use (mongrel or thin)
ADAPTER=mongrel

